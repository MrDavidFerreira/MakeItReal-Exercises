/**
 * 
 * @param {Number} n 
 */
function towerOfHanoi(n) {
    n = parseInt(n);

    let orig = new Stack(); //origin
    let auxi = new Stack(); //
    let dest = new Stack(); //destination

    // fill the a stack
    for (let i = 0; i < n; i++) {
        a.push(i + 1);
    }

    for (let)


}

function towerOfHanoirHelper() {
    
}

towerOfHanoi(5.3)
